Fix this piece of code "@@@ send and error message to client and disconnect instead of" inside flom_daemon.c

Road map:
"no create" flag
"resource lifespan"

implement FIFO, LIFO, FIRST FIT (for numerical resources) lock allocation policies

intercept kill signal from daemon and try to terminate gracefully

IPv6

